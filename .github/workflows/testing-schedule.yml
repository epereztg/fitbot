name: Testing

on:
  schedule:
  - cron: "05 22 * * *"

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v2
      - name: Build docker image
        run: make docker/build
      - name: Run fitbot
        run: docker run -e email=${{ secrets.EMAIL }} -e password=${{ secrets.PASSWORD }} -e booking-goals={'\"0\":{\"time\":\"1815\"\,\"name\":\"ARIBAU\"}\,\"3\":{\"time\":\"2030\"\,\"name\":\"Endurance\"}'} -e box-name=laucalahealthcrossfit -e box-id=9907 -e days-in-advance=1 fitbot
